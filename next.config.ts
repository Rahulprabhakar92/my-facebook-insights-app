/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["platform-lookaside.fbsbx.com"], // 👈 Add the Facebook image domain here
  },
};

module.exports = nextConfig;
